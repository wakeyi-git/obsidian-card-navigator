# Card Navigator 계층 구조 구현 계획

## 1. 목표

사이드바의 제한된 공간에서 **위치 인식**과 **효율적인 탐색**을 제공하는 계층 구조 UI를 구현합니다.

### 핵심 문제
- 카드는 파일 탐색기의 한 줄 목록보다 크기가 커서 시야가 좁음
- 스크롤 시 현재 위치를 잃기 쉬움
- "탐색기"로서의 역할 수행 시 길을 잃는 문제

### 해결 방안
1. **컨텍스트 바 (Context Bar)**: 현재 모드(폴더/태그)에 따른 탐색 인터페이스
2. **컴팩트 계층 헤더**: 최소한의 공간으로 계층 표현 (그룹화 활성화 시)
3. **툴바 단축 버튼**: 그룹화 토글, 핀 토글 등 자주 사용하는 기능 빠른 접근

### 핵심 원칙
- **위치 인식**: 항상 현재 위치를 알 수 있어야 함
- **공간 효율성**: 최소한의 공간으로 최대의 정보
- **직관적 UX**: Obsidian과 일관된 인터랙션
- **점진적 구현**: 각 단계별로 테스트 가능
- **모드 독립성**: Context Bar는 그룹화 설정과 독립적으로 동작

---

## 2. UI 디자인

### 2.1 컨텍스트 바 (Context Bar) - 핵심 탐색 인터페이스

**⚠️ 중요한 설계 변경 (v1.5.3 이후)**

Context Bar는 **그룹화 설정과 독립적으로 항상 표시**됩니다.
- 현재 **모드(폴더/태그)**에 따라 전체 폴더 또는 태그 목록을 드롭다운에 표시
- 클릭 시 해당 폴더/태그로 이동 (specifiedFolder/specifiedTags 설정 변경)
- 네비게이터의 핵심 탐색 기능으로서 역할 수행

```
폴더 모드:
┌─────────────────────────────────────────────────────────┐
│ 📁 현재폴더명 ▼                                         │  ← 컨텍스트 바 (항상 표시)
├─────────────────────────────────────────────────────────┤
│ [카드들...]                                             │
└─────────────────────────────────────────────────────────┘

클릭 시 드롭다운 (전체 폴더 목록):
┌─────────────────────────────────────────────────────────┐
│ 📁 현재폴더명 ▲                                         │
├─────────────────────────────────────────────────────────┤
│  📁 output (10)                                         │
│    📁 하위폴더1 (3)                                     │  ← 계층 들여쓰기
│    📁 하위폴더2 (2)                                     │
│  📁 몸과 세계 (5)                                       │
│  📁 대중서 개작 계획 (3)  ← 현재 위치 강조              │
│  📁 다른 폴더 (7)                                       │
└─────────────────────────────────────────────────────────┘

태그 모드:
┌─────────────────────────────────────────────────────────┐
│ # 현재태그명 ▼                                          │  ← 컨텍스트 바 (항상 표시)
├─────────────────────────────────────────────────────────┤
│ [카드들...]                                             │
└─────────────────────────────────────────────────────────┘

클릭 시 드롭다운 (전체 태그 목록):
┌─────────────────────────────────────────────────────────┐
│ # 현재태그명 ▲                                          │
├─────────────────────────────────────────────────────────┤
│  # project (15)                                         │
│    # project/frontend (8)                               │  ← 계층 들여쓰기
│    # project/backend (5)                                │
│  # daily (20)                                           │
│  # reading (10)  ← 현재 위치 강조                       │
└─────────────────────────────────────────────────────────┘
```

**핵심 기능:**
- **항상 표시**: 그룹화 활성화 여부와 관계없이 항상 표시
- **모드 기반 목록**: 현재 모드(폴더/태그)에 따라 전체 목록 표시
  - 폴더 모드: 전체 폴더 목록 (계층 구조 포함)
  - 태그 모드: 전체 태그 목록 (계층 구조 포함)
- **클릭 시 이동**: 드롭다운에서 항목 클릭 시 해당 폴더/태그의 파일 표시
- **현재 위치 강조**: 현재 선택된 폴더/태그 강조 표시
- **계층 구조 표시**: 폴더/태그의 계층 구조를 들여쓰기로 표현
- **파일 개수 표시**: 각 항목에 파일 개수 `(n)` 표시
- **외부 클릭 시 닫힘**: 드롭다운 외부 클릭 시 자동 닫힘

### 2.2 컴팩트 계층 헤더 (그룹화 활성화 시)

들여쓰기를 최소화한 플랫 스타일:

```
▼ output (10)
▼ 몸과 세계 (5)
▼ 대중서 개작 계획 (3)
  [카드1] [카드2] [카드3]
▶ 다른 폴더 (2)                    ← 접힌 상태
```

**디자인 요소:**
- 들여쓰기 없음 (깊이는 컨텍스트 바로 표현)
- 토글 아이콘: `▶`/`▼` (12px)
- 파일 개수: `(n)` 형식
- 높이: ~24px (최소화)

### 2.3 인터랙션

| 액션 | 동작 |
|------|------|
| 컨텍스트 바 클릭 | 드롭다운 그룹 목록 토글 |
| 드롭다운 폴더/태그 클릭 | 해당 폴더/태그의 파일 표시 (모드 설정 변경) |
| 그룹 헤더 클릭 | 해당 그룹 펼치기/접기 (그룹화 활성화 시) |

### 2.4 툴바 버튼

```
┌─────────────────────────────────────────────────────────┐
│ [📁 Folder] [🔄] [📁/🏷️] [그룹화] [핀] [정렬]     [🔍] │
└─────────────────────────────────────────────────────────┘
  ↑ 모드표시  ↑ 전환 ↑ 옵션  ↑        ↑     ↑        ↑
```

**버튼 순서 (좌→우):**
1. **모드 표시**: 현재 모드와 폴더/태그명 표시
2. **모드 전환** (`repeat`): 폴더 ↔ 태그 모드 전환
3. **모드 옵션** (`folder`/`folder-open` 또는 `tags`): 활성/지정 모드 전환
4. **그룹화 토글** (`layout-grid`/`layout-list`): 그룹화 활성화/비활성화
5. **핀 토글** (`pin`/`pin-off`): 핀된 파일만 보기/전체 보기
6. **정렬** (`arrow-up-down`): 정렬 방식 변경
7. **검색** (`search`): 검색창 토글

---

## 3. 데이터 구조

### 3.1 CardGroup 인터페이스 확장

```typescript
export interface CardGroup {
    id: string;                    // 고유 ID (예: "folder-path/to/folder")
    name: string;                  // 표시 이름 (예: "folder")
    fullPath: string;              // 전체 경로 (예: "path/to/folder")
    icon: string;                  // 아이콘
    files: TFile[];                // 직계 파일만 포함
    collapsed: boolean;            // 접힘 상태
    sortKey: string | number;      // 정렬 키

    // 계층 구조 필드 (옵셔널)
    level?: number;                // 깊이 (0 = 루트)
    parentId?: string | null;      // 부모 그룹 ID
    children?: CardGroup[];        // 자식 그룹

    // 메타데이터
    totalFileCount?: number;       // 모든 하위 포함 총 파일 수
}
```

### 3.2 그룹 목록 아이템 (드롭다운용)

```typescript
interface GroupListItem {
    id: string;           // 그룹 ID (folder-{path} 또는 tag-{tag})
    name: string;         // 그룹명
    icon: string;         // 아이콘 (folder, hash, etc.)
    fileCount: number;    // 파일 개수
    isActive: boolean;    // 현재 활성 그룹 여부
    level?: number;       // 계층 레벨 (들여쓰기용)
    hasChildren?: boolean; // 자식 항목 여부
    parentId?: string;    // 부모 ID
}
```

---

## 4. 구현 단계

### Phase 1: 컨텍스트 바 드롭다운 구현 ✅ (완료)

**목표**: 모드 기반 탐색 인터페이스

**현재 문제점:**
- Context Bar가 그룹화 활성화 시에만 표시됨
- 드롭다운이 현재 모드(폴더/태그)와 무관하게 항상 폴더 목록만 표시

**필요한 수정:**
- Context Bar를 항상 표시 (그룹화 설정과 독립)
- 드롭다운 목록을 현재 모드(currentMode)에 따라 동적으로 변경
- 폴더 모드: getAllFolders() 사용
- 태그 모드: getAllTags() 사용

**파일 변경:**
- `src/ui/ContextBar.ts`: 항상 표시, 모드 기반 아이콘 변경
- `src/view.ts`: Context Bar 초기화 로직 변경
- `src/view/ViewRenderer.ts`: updateContextBarGroupList 로직 수정
- `styles.css`: 스타일 유지

**테스트:**
- [ ] Context Bar 항상 표시
- [ ] 폴더 모드에서 폴더 목록 표시
- [ ] 태그 모드에서 태그 목록 표시
- [ ] 드롭다운 항목 클릭 시 해당 폴더/태그로 이동
- [ ] 현재 위치 강조 표시
- [ ] 모드 전환 시 드롭다운 목록 변경

---

### Phase 2: 툴바 단축 버튼 구현 ✅ (완료)

**목표**: 자주 사용하는 기능 빠른 접근

**파일 변경:**
- `src/ui/Toolbar.ts`: 그룹화/핀 토글 버튼 추가
- `styles.css`: 툴바 버튼 스타일

**테스트:**
- [x] 그룹화 토글 버튼 동작
- [x] 핀 토글 버튼 동작
- [x] 활성 상태 시각적 표시
- [x] 기존 기능 호환성

---

### Phase 3: 데이터 모델 확장 ✅ (완료)

**목표**: 계층 구조 데이터 준비

**파일 변경:**
- `src/types.ts`: CardGroup 확장 ✅
  - `fullPath`: 전체 경로 추가
  - `level`: 계층 깊이 추가
  - `parentId`: 부모 그룹 ID 추가
  - `children`: 자식 그룹 배열 추가
  - `totalFileCount`: 하위 포함 총 파일 수 추가
- `src/grouping/GroupingManager.ts`: ✅
  - 모든 그룹화 메서드에 새 필드 적용
  - `buildHierarchy()`: 플랫 그룹을 트리로 변환
  - `flattenHierarchy()`: 트리를 플랫 배열로 펼침 (Phase 4용)
  - `expandAncestors()`: 활성 파일의 조상 그룹 펼침

**테스트:**
- [x] 빌드 성공 확인
- [x] 기존 플랫 모드 호환성 (1245개 테스트 통과)

---

### Phase 4: 계층 렌더링 ✅ (완료)

**목표**: 트리 구조 UI 렌더링

**파일 변경:**
- `src/grouping/GroupRenderer.ts`: ✅
  - `createHierarchicalGroupSection()`: 계층 구조 그룹 섹션 생성
  - `toggleHierarchicalGroup()`: 부모 접힘 시 자식도 숨김
  - `checkHasActiveFile()`: 활성 파일 포함 여부 재귀 확인
  - `hideDescendants()`: 후손 그룹 숨김
  - `findChildrenContainer()`, `findChildSections()`: 자식 요소 검색
- `src/view/ViewRenderer.ts`: ✅
  - `prepareGroups()`: 계층 모드 감지 및 buildHierarchy() + flattenHierarchy() 호출
  - `findGroupIdForFile()`: 파일의 그룹 ID 검색
  - `onHierarchicalGroupToggle()`: 계층 구조 토글 핸들러

**테스트:**
- [x] 중첩 그룹 렌더링
- [x] 펼치기/접기 동작
- [x] 부모 접힘 시 자식 숨김

---

### Phase 5: 스타일 및 최적화 ✅ (완료)

**목표**: UI 마무리, 성능 최적화

**파일 변경:**
- `src/grouping/GroupRenderer.ts`: ✅
  - `INDENT_SIZE`: 16px → 12px로 축소 (컴팩트 들여쓰기)
- `styles.css`: ✅
  - 계층 구조 스타일 최적화 (Phase 4 & 5)
  - 컴팩트 패딩 (4px)
  - 레벨별 폰트 크기 및 투명도 조정
  - 그룹명 말줄임 (max-width: 150px)
  - 호버 시 테두리 강조
  - 가로 모드 대응
- `src/i18n/locales/*.ts`: ✅ (이미 완료)
  - `sortGroupsOptions.hierarchy`: 'Hierarchy' / '계층 구조' 번역 추가

**테스트:**
- [x] 다크/라이트 테마
- [x] 가로/세로 모드
- [x] 컴팩트 들여쓰기 적용
- [x] 다국어 번역

---

## 5. UI 목업

### 5.1 기본 상태 (세로 모드) - 폴더 모드

```
┌─────────────────────────────────────────┐
│ [📁 Folder] [🔄][📁][그룹][핀][정렬][🔍]│  ← 툴바
├─────────────────────────────────────────┤
│ 📁 대중서 개작 계획 ▼                   │  ← 컨텍스트 바 (항상 표시)
├─────────────────────────────────────────┤
│ ┌─────────┐ ┌─────────┐ ┌─────────┐    │
│ │ 카드 1  │ │ 카드 2  │ │ 카드 3  │    │  ← 그룹화 비활성화
│ └─────────┘ └─────────┘ └─────────┘    │
└─────────────────────────────────────────┘
```

### 5.2 드롭다운 펼침 상태 - 폴더 모드

```
┌─────────────────────────────────────────┐
│ [📁 Folder] [🔄][📁][그룹][핀][정렬][🔍]│
├─────────────────────────────────────────┤
│ 📁 대중서 개작 계획 ▲                   │
├─────────────────────────────────────────┤
│  📁 Root (5)                            │
│  📁 output (10)                         │
│    📁 하위폴더 (3)                      │
│  📁 대중서 개작 계획 (3)  ← 강조        │
│  📁 다른 폴더 (7)                       │
├─────────────────────────────────────────┤
│ [카드들...]                             │
└─────────────────────────────────────────┘
```

### 5.3 태그 모드

```
┌─────────────────────────────────────────┐
│ [# Tag]  [🔄][🏷️][그룹][핀][정렬][🔍]  │  ← 툴바
├─────────────────────────────────────────┤
│ # reading ▼                             │  ← 컨텍스트 바 (태그 아이콘)
├─────────────────────────────────────────┤
│ [카드들...]                             │
└─────────────────────────────────────────┘

드롭다운:
├─────────────────────────────────────────┤
│  # daily (20)                           │
│  # project (15)                         │
│    # project/frontend (8)               │
│    # project/backend (5)                │
│  # reading (10)  ← 강조                 │
│  # todo (8)                             │
└─────────────────────────────────────────┘
```

---

## 6. 파일 변경 목록

| 파일 | 변경 내용 | Phase | 상태 |
|------|----------|-------|------|
| `src/ui/ContextBar.ts` | 항상 표시, 모드 기반 동작 | 1 | 🔄 재구현 필요 |
| `src/view.ts` | Context Bar 초기화 로직 | 1 | 🔄 재구현 필요 |
| `src/view/ViewRenderer.ts` | updateContextBarGroupList 수정, 계층 렌더링 | 1, 4 | ✅ 완료 (Phase 4) |
| `styles.css` | 컨텍스트 바 + 드롭다운 + 계층 구조 스타일 | 1, 2, 5 | ✅ 완료 |
| `src/ui/Toolbar.ts` | 그룹화/핀 토글 버튼 | 2 | ✅ 완료 |
| `src/types.ts` | CardGroup 확장 | 3 | ✅ 완료 |
| `src/grouping/GroupingManager.ts` | buildHierarchy() | 3 | ✅ 완료 |
| `src/grouping/GroupRenderer.ts` | 재귀적 렌더링, INDENT_SIZE 축소 | 4, 5 | ✅ 완료 |
| `src/i18n/locales/*.ts` | 번역 | 5 | ✅ 완료 |

---

## 7. 테스트 체크리스트

### 기능 테스트 - Phase 1 (재구현 필요)
- [ ] Context Bar 항상 표시 (그룹화 여부 무관)
- [ ] 폴더 모드에서 폴더 목록 표시
- [ ] 태그 모드에서 태그 목록 표시
- [ ] 드롭다운 항목 클릭 시 해당 폴더/태그로 이동
- [ ] 현재 위치 강조 표시
- [ ] 계층 구조 들여쓰기 표시
- [ ] 외부 클릭 시 드롭다운 닫힘
- [ ] 모드 전환 시 드롭다운 목록 변경

### 기능 테스트 - Phase 2 (완료)
- [x] 툴바 그룹화 토글 버튼
- [x] 툴바 핀 토글 버튼
- [x] 버튼 활성 상태 시각적 표시

### 기능 테스트 - Phase 3 (완료)
- [x] CardGroup 인터페이스 확장 (fullPath, level, parentId, children, totalFileCount)
- [x] 모든 그룹화 메서드에 새 필드 적용
- [x] buildHierarchy() 메서드 구현
- [x] flattenHierarchy() 메서드 구현
- [x] expandAncestors() 메서드 구현
- [x] 빌드 성공 확인
- [x] 기존 플랫 모드 호환성 (1245개 테스트 통과)

### 기능 테스트 - Phase 4 (완료)
- [x] 폴더 계층 구조 렌더링
- [x] 태그 계층 구조 렌더링
- [x] 개별 그룹 펼치기/접기
- [x] 부모 접힘 시 자식 숨김
- [ ] 상태 저장 및 복원
- [ ] 활성 파일 그룹 자동 펼치기

### 기능 테스트 - Phase 5 (완료)
- [x] 컴팩트 들여쓰기 (12px per level)
- [x] 레벨별 폰트 크기/투명도
- [x] 그룹명 말줄임
- [x] 호버 시 테두리 강조
- [x] 가로 모드 대응

### 성능 테스트
- [ ] 1000+ 파일 렌더링 속도
- [ ] 스크롤 부드러움
- [ ] 펼치기/접기 반응성

### 호환성 테스트
- [x] 기존 플랫 그룹화 정상 작동
- [ ] Context Bar 항상 표시
- [x] 세로/가로 모드
- [x] 다크/라이트 테마

---

## 8. 롤백 계획

1. 각 Phase는 독립 커밋으로 관리
2. 문제 발생 시 해당 Phase만 되돌리기
3. `groupSort: 'hierarchy'` 설정으로 기능 on/off
4. Context Bar는 항상 표시 (롤백 불필요)

---

## 9. 진행 상황

| Phase | 내용 | 상태 |
|-------|------|------|
| 1 | 컨텍스트 바 드롭다운 | 🔄 재구현 필요 |
| 2 | 툴바 단축 버튼 | ✅ 완료 |
| 3 | 데이터 모델 확장 | ✅ 완료 |
| 4 | 계층 렌더링 | ✅ 완료 |
| 5 | 스타일/최적화 | ✅ 완료 |

### 변경 이력
- **v1.5.2**: Sticky Header 제거, Context Bar 드롭다운 방식으로 변경, 툴바 그룹화/핀 버튼 추가
- **v1.5.3**: Phase 3 완료 - CardGroup 데이터 모델 확장, buildHierarchy() 구현
- **v1.5.4**: Phase 4 완료 - 계층 구조 렌더링 (GroupRenderer, ViewRenderer)
- **v1.5.5**: Phase 5 완료 - 스타일 최적화 (INDENT_SIZE 12px, 컴팩트 스타일, 레벨별 폰트 크기)
